import React from "react";
import Controls from "./controls";

const nav = () => (
  <div className="nav">
    <h1>Keeps</h1>
    <div className="note-controls">
      <Controls />
    </div>
  </div>
);

export default nav;
